package api_requests

const modelName = "meta-llama/Llama-3.2-11B-Vision-Instruct"

const chunksFileName = "texts.json"

const url = "https://innoglobalhack-general.olymp.innopolis.university/"

const tokenizeUrl = url + "tokenize"
const embeddingsUrl = url + "embeddings"
const detokenizeUrl = url + "embeddings"
